let display = () =>
{
    const a = 1
    const b = 2

    if (a < b)
        console.log(`${a} is less than ${b}`)
    else
        console.log(`${b} is less than ${a}`)
}
export default display